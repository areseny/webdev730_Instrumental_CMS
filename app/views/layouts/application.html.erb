<!DOCTYPE html>
<html id="top">
<head>
  <title><%= t "title" %></title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= fontawesome_asset_tags %>
  <%= csrf_meta_tags %>
  <%= facebook_meta_tags %>
  <%= recaptcha_meta_tags %>
  <%= render "live_reload" if Rails.env.development? %>
</head>
<body>

  <div class="layout-background">
    <div class="layout-wrapper">
      <%= render "header" %>
      <div class="layout-content">
        <%= content_for?(:content) ? yield(:content) : yield %>
      </div>
    </div>
    <%= render "newsletter_signup_form" %>
    <%= render "feeds" %>
    <%= render "footer" %>
  </div>

</body>
</html>
